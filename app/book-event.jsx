import { StyleSheet, Text, View } from "react-native";

const BookEvent = () => {
	return (
		<View>
			<Text>BookEvent</Text>
		</View>
	);
};

export default BookEvent;

const styles = StyleSheet.create({});
